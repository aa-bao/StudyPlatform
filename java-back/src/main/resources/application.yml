server:
  port: 8081
  tomcat:
    max-http-form-post-size: 50MB
    max-swallow-size: 50MB

spring:
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    jdbc-url: jdbc:mysql://localhost:3306/kaoyan_platform?serverTimezone=GMT%2B8&useUnicode=true&characterEncoding=utf-8
    username: root
    password: 123456
  mail:
    host: smtp.qq.com
    port: 587
    username: 2847202298@qq.com
    password: sskpeupsllwoddbj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

# 邮件验证码配置
mail:
  code:
    expireMinutes: 5
    maxSendCount: 5
    intervalSeconds: 60

mybatis-plus:
  configuration:
    map-underline-to-camel-case: true
  global-config:
    db-config:
      id-type: auto
  mapper-locations: classpath*:/mapper/**/*.xml

python:
  backend:
    url: http://localhost:8082
    timeout: 30000
    enabled: true  # 启用 Python 后端